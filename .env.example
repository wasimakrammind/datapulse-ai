# Database (SQLite - no setup needed)
DB_PATH=data/datapulse.db

# API
API_KEY=datapulse-dev-key-2024
API_HOST=0.0.0.0
API_PORT=8000

# GenAI (optional - rule-based fallback works without this)
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini

# Pipeline Config
SLA_RUNTIME_SECONDS=300
ANOMALY_VOLUME_THRESHOLD=0.3
